test_package::tf_static_broadcaster_only:
  _jig_dummy:
    type: bool
    default_value: true
    description: Dummy parameter (jig generates this when no parameters are defined)
    read_only: true
